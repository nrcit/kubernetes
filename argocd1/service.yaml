apiVersion: v1
kind: Service
metadata:
  name: sv-nc-app
spec:
  selector:
    app: pod-nc-app
  type: LoadBalancer                      # ClusterIP, NodePort, LoadBalancer, ExternalName
  ports:
  - nodePort: 30005
    port: 80
    targetPort: 80